import React from "react";
import ProductResults from "../../components/ProductResults";

const Shop = ({}) => {
  return (
    <div className="shopPage">
      <ProductResults />
    </div>
  );
};

export default Shop;
